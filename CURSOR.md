# Using this repo with Cursor

The cap discipline applies similarly to Cursor's rules and memory systems. Cursor doesn't publish the exact same caps as Anthropic, but the principle holds: leaner is better.

Copy `.cursor/rules/claude-md-overhaul.mdc` for Cursor, or `CLAUDE.md` to Claude Code projects.
